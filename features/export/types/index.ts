export interface ExportFormat {
  format: string;
  ext: string;
  desc: string;
  icon: any;
}

export interface ModelMetadataItem {
  label: string;
  value: string;
}

export interface AdvancedOption {
  label: string;
  checked: boolean;
}
