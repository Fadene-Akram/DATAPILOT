export function PageHeader() {
  return (
    <div>
      <h1 className="text-3xl font-bold text-foreground mb-2">
        Model Building
      </h1>
      <p className="text-muted-foreground">
        Select a task type, choose algorithms, configure hyperparameters, and
        train models
      </p>
    </div>
  );
}
